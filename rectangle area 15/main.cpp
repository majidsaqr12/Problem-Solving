#include <iostream>

using namespace std;

int main()
{
    float width, length;

    cout<< "Enter width and length of rectangle ? ";
    cin>> width >> length;

    float area = width * length;

    cout << "The area of ractangle is " << area;
    return 0;
}
